var str = "Developer";
var reverseStr = "";
for (let i = str.length - 1; i >= 0; i--) {
  reverseStr = reverseStr + str[i];
}
console.log(reverseStr);

var str = "Developer";
let res = "";
for (let i = str.length - 1; i >= 0; i--) {
    res = res + str[i]
}
console.log(res);
